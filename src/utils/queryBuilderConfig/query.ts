import { RuleGroupType } from "react-querybuilder";
export const initialQueryIodef: RuleGroupType = {
  combinator: "and",
  rules: [],
};
export const initialQuery: RuleGroupType = {
  combinator: "and",
  rules: [
    // { field: "x_short_id", operator: "=", value: "" },
    // { field: "field", value: "text", valueSource: "field", operator: "=" },
  ],
};
export const initialAdvanceFilter: RuleGroupType = {
  combinator: "and",
  rules: [],
};

export const initialQueryStix: RuleGroupType = {
  combinator: "and",
  rules: [],
};
